# Database Viewer (view-db)

Тестовая программа для просмотра и управления данными в PostgreSQL базе данных (Neon).

## Возможности

- ✅ Выбор базы данных (локальная или рабочая)
- ✅ Просмотр списка всех таблиц в базе данных
- ✅ Просмотр данных таблицы с пагинацией
- ✅ CRUD операции:
  - **Create** - создание новых записей
  - **Read** - просмотр данных с пагинацией
  - **Update** - редактирование записей
  - **Delete** - удаление записей

## Доступ

Откройте в браузере: **http://localhost:3000/view-db**

Или перейдите по ссылке "Database Viewer" на главной странице.

## Настройка

### Переменные окружения

Для работы с двумя базами данных добавьте в `.env`:

```env
# Локальная база данных
DATABASE_URL="postgresql://user:password@host:5432/database?sslmode=require"

# Рабочая база данных (опционально)
DATABASE_URL_PROD="postgresql://user:password@host:5432/database?sslmode=require"
```

Если `DATABASE_URL_PROD` не указан, рабочая БД будет использовать `DATABASE_URL`.

## Использование

### 1. Выбор базы данных

На главной странице `/view-db` выберите:
- **Локальная БД** - использует `DATABASE_URL`
- **Рабочая БД** - использует `DATABASE_URL_PROD` (или `DATABASE_URL` если не указан)

### 2. Просмотр таблиц

После выбора БД отобразится список всех таблиц в схеме `public`. Для каждой таблицы есть кнопка **"Открыть"**.

### 3. Просмотр данных таблицы

После нажатия "Открыть" откроется страница с:
- Полным списком колонок и их типами
- Данными таблицы с пагинацией (10 записей на страницу)
- Навигацией по страницам

### 4. CRUD операции

#### Create (Создать)
- Нажмите кнопку **"+ Создать запись"**
- Заполните форму с полями таблицы
- Нажмите **"Сохранить"**

#### Update (Обновить)
- Нажмите кнопку **✏️** рядом с записью
- Отредактируйте значения в полях
- Нажмите **✓** для сохранения или **✕** для отмены

#### Delete (Удалить)
- Нажмите кнопку **🗑️** рядом с записью
- Подтвердите удаление

## Технические детали

### Структура файлов

```
app/
  view-db/
    page.tsx              # Главная страница (выбор БД и список таблиц)
    [table]/
      page.tsx            # Страница просмотра таблицы с CRUD
  api/
    view-db/
      tables/route.ts     # API: получение списка таблиц
      table/route.ts      # API: получение данных таблицы
      create/route.ts     # API: создание записи
      update/route.ts     # API: обновление записи
      delete/route.ts     # API: удаление записи
lib/
  db-utils.ts             # Утилиты для работы с БД
```

### Безопасность

⚠️ **Важно**: Эта программа предназначена для разработки и тестирования. 

- Использует raw SQL запросы для работы с любыми таблицами
- Экранирует значения для защиты от SQL инъекций
- Не рекомендуется использовать в production без дополнительной авторизации

### Ограничения

- Работает только с таблицами в схеме `public`
- Первая колонка используется как primary key для операций Update/Delete
- Пагинация: 10 записей на страницу
- Поддерживаются только базовые типы данных PostgreSQL

## Примеры использования

### Просмотр таблицы notes

1. Откройте `/view-db`
2. Выберите "Локальная БД"
3. Найдите таблицу `notes` и нажмите "Открыть"
4. Просмотрите данные с пагинацией

### Создание новой записи

1. На странице таблицы нажмите "+ Создать запись"
2. Заполните поля (например, для таблицы `notes`: `title`)
3. Нажмите "Сохранить"

### Редактирование записи

1. Нажмите ✏️ рядом с записью
2. Измените значения в полях
3. Нажмите ✓ для сохранения

### Удаление записи

1. Нажмите 🗑️ рядом с записью
2. Подтвердите удаление
